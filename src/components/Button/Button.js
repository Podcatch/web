import React from 'react'

class Button extends React.Component {
    render() {
        return (
            <div>
                <button>Login</button>
            </div>
        )
    }
}

export default Button